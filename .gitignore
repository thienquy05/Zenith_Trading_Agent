# Claude Code config: share team-wide settings + hooks, keep local overrides out
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json

# codebase-memory-mcp local graph artifacts + auto-index state
.codebase-memory/

.env

# Quy Dashboard artifact source — contains real Robinhood balances/positions.
# Built locally each session and published privately via the Artifact tool;
# never committed to the repo. See AGENT-INSTRUCTIONS.md "Dashboard procedure".
dashboard/quy-dashboard.html

# Python
__pycache__/
*.pyc
.venv/
.venv_check/

# Node / Next.js
node_modules/
.next/

# Docker
postgres_data/
