# Claude Code config: share team-wide settings + hooks, keep local overrides out
.claude/*
!.claude/settings.json
!.claude/hooks/
.claude/settings.local.json

# codebase-memory-mcp local graph artifacts + auto-index state
.codebase-memory/

.env

# Dashboard is worked on locally only — regenerated + published as an
# Artifact each run, no longer committed to git (see AGENT-INSTRUCTIONS.md)
dashboard/quy-dashboard.html

# Python
__pycache__/
*.pyc
.venv/
.venv_check/

# Node / Next.js
node_modules/
.next/

# Docker
postgres_data/
